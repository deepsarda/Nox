---
description: Format a .nox file in place with `noxfmt`.
---

Run `noxfmt {{args}}`. Report which files were changed. If `--check` was passed,
exit code 1 means formatting drift; list the drifting files.
