/**
 * Virtual machine:
 *  VirtualMachine: main execution loop (fetch-decode-execute)
 *  Opcodes: opcode constants (IADD, DADD, CALL, SCALL, HMOD, …)
 *  Memory: dual-bank register file (pMem / rMem), sliding window frames
 */
package nox.vm
